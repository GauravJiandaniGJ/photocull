# macOS
.DS_Store

# Xcode — the project is regenerated from project.yml with `xcodegen generate`
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
build/
*.xcarchive
*.ipa
*.dSYM.zip

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Claude Code local state
.claude/settings.local.json
.claude/worktrees/

# Secrets — never commit signing keys or provisioning profiles
*.p8
*.p12
*.mobileprovision
